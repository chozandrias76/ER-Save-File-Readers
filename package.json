{
  "name": "rust-wasm-project",
  "version": "1.0.0",
  "scripts": {
    "build": "wasm-pack build --target web",
    "vercel-build": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "wasm-pack": "^0.10.2"
  }
}
