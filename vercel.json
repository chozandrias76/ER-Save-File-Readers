{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "pkg"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/pkg/$1"
    }
  ]
}
